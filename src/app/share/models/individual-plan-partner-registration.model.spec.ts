import { IndividualPlanPartnerRegistration } from './individual-plan-partner-registration.model';

describe('IndividualPlanPartnerRegistration', () => {
  it('should create an instance', () => {
    expect(new IndividualPlanPartnerRegistration()).toBeTruthy();
  });
});
