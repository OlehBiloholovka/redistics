import { ActivationIndicator } from './activation-indicator.model';

describe('ActivationIndicator', () => {
  it('should create an instance', () => {
    expect(new ActivationIndicator()).toBeTruthy();
  });
});
