import { OutletRegistration } from './outlet-registration.model';

describe('OutletRegistration', () => {
  it('should create an instance', () => {
    expect(new OutletRegistration()).toBeTruthy();
  });
});
