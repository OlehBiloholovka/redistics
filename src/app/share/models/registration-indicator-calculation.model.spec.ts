import { RegistrationIndicatorCalculation } from './registration-indicator-calculation.model';

describe('IndicatorCalculation', () => {
  it('should create an instance', () => {
    expect(new RegistrationIndicatorCalculation()).toBeTruthy();
  });
});
