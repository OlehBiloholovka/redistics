import { PartnerRegistration } from './partner-registration.model';

describe('PartnerRegistration', () => {
  it('should create an instance', () => {
    expect(new PartnerRegistration()).toBeTruthy();
  });
});
