import { RegistrationCounter } from './registration-counter';

describe('RegistrationCounter', () => {
  it('should create an instance', () => {
    expect(new RegistrationCounter()).toBeTruthy();
  });
});
