import {DashboardProgressBarCreator} from './dashboard-progress-bar-creator';

describe('DashboardProgressBarCreator', () => {
  it('should create an instance', () => {
    expect(new DashboardProgressBarCreator()).toBeTruthy();
  });
});
