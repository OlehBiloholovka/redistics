import {DashboardProgressBar} from './dashboard-progress-bar.model';

describe('DashboardProgressBar', () => {
  it('should create an instance', () => {
    expect(new DashboardProgressBar()).toBeTruthy();
  });
});
